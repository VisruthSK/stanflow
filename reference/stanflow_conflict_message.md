# Generate conflict message

Adapted from the helper code behind `tidyverse::tidyverse_conflicts()`.

## Usage

``` r
stanflow_conflict_message(x)
```

## Arguments

- x:

  A named list describing function-name conflicts.

## Value

Character vector of the conflict message to print.
