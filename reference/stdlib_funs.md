# Standard-library function names to never attribute to Stan packages

This includes exports from: base, stats, utils, graphics, grDevices,
methods. This is generated in `data_raw/sysdata.R` and exported for
documentation/transparency.

## Usage

``` r
stdlib_funs()
```

## Value

Character vector of standard-library function names.
