# Default directory names skipped by scan_usage/stan_cite

Default directory names skipped by scan_usage/stan_cite

## Usage

``` r
scan_skip_dirs()
```

## Value

Character vector of directory names to skip.
