% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interfaces.R
\name{setup_cmdstanr}
\alias{setup_cmdstanr}
\title{Setup cmdstanr and CmdStan}
\usage{
setup_cmdstanr(
  quiet,
  force,
  reinstall = FALSE,
  check_updates = FALSE,
  cores,
  dry_run = FALSE
)
}
\arguments{
\item{quiet}{Logical. If \code{TRUE}, suppresses status messages. This cannot suppress cmdstan messages.}

\item{force}{Logical. If \code{TRUE}, allows installation in non-interactive sessions.}

\item{reinstall}{Logical. If \code{TRUE}, forces re-installation.}

\item{check_updates}{Logical. If \code{TRUE}, checks for CmdStan updates.}

\item{cores}{Integer. Number of cores to use. Defaults to
\code{getOption("mc.cores")}. You must set \code{options(mc.cores = ...)} or pass
\code{cores} explicitly.}

\item{dry_run}{Logical. If \code{TRUE}, prints the setup actions that would run
without installing packages, attaching packages, changing options, fixing
the toolchain, or installing CmdStan. Defaults to \code{FALSE}.}
}
\value{
Returns \code{TRUE} invisibly when no install/upgrade is needed.
Otherwise, returns \code{NULL} invisibly after installation.
}
\description{
Checks the C++ toolchain, locates CmdStan, and installs or upgrades
CmdStan if needed. Prefer \code{setup_interface()} for user-facing setup since
it performs argument validation and defaults; \code{setup_cmdstanr()} assumes
inputs are already checked.
}
\examples{
\dontrun{
setup_cmdstanr(
  quiet = TRUE,
  force = TRUE,
  reinstall = FALSE,
  check_updates = FALSE,
  cores = 2
)
}
}
