% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interfaces.R
\name{setup_brms}
\alias{setup_brms}
\title{Setup brms}
\usage{
setup_brms(quiet, brms_backend, cores)
}
\arguments{
\item{quiet}{Logical. If \code{TRUE}, suppresses status messages. This cannot suppress cmdstan messages.}

\item{brms_backend}{Character. The \code{brms} backend to use. Defaults to
\code{getOption("brms.backend", "cmdstanr")} and must be one of
\code{c("cmdstanr", "rstan")}.}

\item{cores}{Integer. Number of cores to use. Defaults to
\code{getOption("mc.cores")}. You must set \code{options(mc.cores = ...)} or pass
\code{cores} explicitly.}
}
\value{
Returns \code{NULL} invisibly.
}
\description{
Configures \code{brms} to use available cores and sets the backend.
Prefer \code{setup_interface()} for user-facing setup since it performs argument
validation and defaults; \code{setup_brms()} assumes inputs are already checked.
}
