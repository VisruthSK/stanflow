% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/funscanr.R
\name{scan_usage}
\alias{scan_usage}
\title{Find used functions and packages}
\usage{
scan_usage(
  path = ".",
  ignore_unqualified_functions = .stdlib_funs,
  strict = FALSE,
  skip_dirs = .scan_skip_dirs,
  allowed_packages = .stan_pkgs,
  export_index = .stan_export_index,
  origin_map = .stan_origin_map,
  quiet = getOption("stanflow.quiet", FALSE)
)
}
\arguments{
\item{path}{A single project directory (searched recursively) or a vector of
files (.R/.Rmd/.qmd).}

\item{ignore_unqualified_functions}{Defaults to exports from base R packages
listed in \code{stdlib_funs()}. Character vector of function names to ignore when
attributing (unqualified) calls to Stan packages. Calls like \code{rstan::plot()}
will NOT be ignored even if \code{plot} is in \code{ignore_unqualified_functions}, since
they are namespaced.}

\item{strict}{If \code{TRUE}, only count unqualified function calls that resolve
to a single Stan package.}

\item{skip_dirs}{Character vector of directory names to skip when scanning a
directory. Defaults to \code{.scan_skip_dirs}.}

\item{allowed_packages}{Character vector of package namespaces to attribute
to Stan usage. Defaults to \code{.stan_pkgs}.}

\item{export_index}{Named list mapping function names to packages. Defaults
to \code{.stan_export_index}.}

\item{origin_map}{Named character vector mapping \code{pkg::fun} keys to the
origin package. Defaults to \code{.stan_origin_map}.}

\item{quiet}{Logical. If \code{TRUE}, suppresses status messages.}
}
\value{
A list of packages, resolved functions, and ambiguous function calls.
}
\description{
This function is primarily exported for developers, and
is almost entirely divested from \code{stanflow}, except for
defaults and \code{{stanflow}} behavior explained later.
}
\details{
The scanning is wholly static (AST parsing), so there are
a number of restrictions on what calls are recognized:
calls to \code{library()}, \code{require()}, \code{requireNamespace()},
or \code{use()} are all recognized as attaching a package.

The Stan values for \code{allowed_packages}, \code{export_index}, and \code{origin_map}
are generated by \code{data_raw/sysdata.R}.

Unqualified function calls are only attributed when a Stan package is
attached via \code{library()} or \code{require()} in the same file. Attaching
\code{{stanflow}} is hardcoded as attaching its core packages (\code{bayesplot}, \code{loo},
\code{posterior}, \code{projpred}, \code{shinystan}). Known reexports are remapped to their
origin packages; missing mappings fall back to the resolved package.
}
\examples{
path <- tempfile(fileext = ".R")
writeLines(
  c(
    "# one messy analysis file",
    "library(posterior)",
    "requireNamespace(\"brms\")",
    "use(\"cmdstanr\", c(\"cmdstan_model\", \"write_stan_json\"))",
    "draws <- as_draws(list(mu = rnorm(10)))",
    "posterior::rhat(draws)",
    "brms::mixture(0.4)",
    "cmdstanr::write_stan_json(list(N = 3), \"data.json\")"
  ),
  path
)
scan_usage(path, quiet = TRUE)
unlink(path)
}
