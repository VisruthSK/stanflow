# stanflow_conflict_message renders deterministic output

    -- Conflicts ------------------------------------------- stanflow_conflicts() --
    x stanflow::filter() masks dplyr::filter(), stats::filter()
    x stanflow::select() masks dplyr::select()
    i Use the conflicted package (<http://conflicted.r-lib.org/>) to force all conflicts to become errors

# print.stanflow_conflicts returns input invisibly

    -- Conflicts ------------------------------------------- stanflow_conflicts() --
    x stanflow::compare() masks stats::compare()
    i Use the conflicted package (<http://conflicted.r-lib.org/>) to force all conflicts to become errors

